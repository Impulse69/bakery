{
  "name": "@bakery/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'Building types...'",
    "dev": "echo 'Starting types dev...'",
    "lint": "tsc --noEmit --strict --esModuleInterop --skipLibCheck --forceConsistentCasingInFileNames src/index.ts",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true});\""
  }
}
